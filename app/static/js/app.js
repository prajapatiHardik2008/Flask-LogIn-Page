document.querySelectorAll('.toggle-password').forEach((button) => { button.addEventListener('click', () => { const input = button.previousElementSibling; const showing = input.type === 'text'; input.type = showing ? 'password' : 'text'; button.textContent = showing ? 'Show' : 'Hide'; button.setAttribute('aria-label', showing ? 'Show password' : 'Hide password'); }); });
const password = document.querySelector('input[name="password"]'); const strength = document.querySelector('#strength'); if (password && strength) password.addEventListener('input', () => { let score = 0; if (password.value.length >= 8) score += 25; if (/[A-Z]/.test(password.value)) score += 25; if (/[0-9]/.test(password.value)) score += 25; if (/[^A-Za-z0-9]/.test(password.value)) score += 25; strength.style.setProperty('--strength', `${score}%`); });
document.querySelectorAll('form').forEach((form) => form.addEventListener('submit', () => { const button = form.querySelector('[data-loading]'); if (button) { button.disabled = true; button.textContent = button.dataset.loading; } }));
